# 决策记录

- 2026-04-02：按照“最佳最小方式”收口 Phase 17，不扩展额外接口，只验证并保留已经接入的多租户、加密存储、增量同步、GraphQL API 四项能力。
- 2026-04-02：真实验证采用 `cargo test -p evif-rest --test multi_tenant --test encryption_at_rest --test incremental_sync --test graphql_api -- --nocapture`，以 Phase 17 对应 19 个集成测试作为完成标准。
- 2026-04-02：补充 `GET /api/v1/tenants/:id` 集成测试，Phase 17 实际验证标准更新为 20 个集成测试全部通过。
- 2026-04-02：同步修正 mem14.md 中前文残留的“需实现 / 待实现 / 规划中 / 未来规划”旧表述，统一为与当前实现状态一致的历史描述。
- 2026-04-02：全库生产级分析结论写入 mem15.md。功能实现进度按 mem14.md 严格口径评估为 98.3%，生产成熟度评估为 2.9/5（约 58%），后续优先级为“先过严格门禁，再补观测，再补状态可靠性与部署闭环”。
- 2026-04-02：按 mem15.md 的最佳最小实现优先选择 Phase B 而非 Phase A，因为“让 TrafficStats 成为可信信号”可以形成独立闭环，风险和改动面都明显小于全仓 clippy 清零。
- 2026-04-02：新增 TrafficMetricsMiddleware，将真实请求统计接入 TrafficStats；新增 metrics_traffic.rs 集成测试，真实验证命令为 `cargo test -p evif-rest --test metrics_traffic metrics_traffic_counts_real_requests -- --nocapture` 和 `cargo test -p evif-rest --lib --tests --quiet`。
- 2026-04-02：Phase B 第二个最小子项选择“标准 `/metrics` Prometheus 文本接口”而不是 tracing 初始化，因为它可在现有接线基础上形成独立可抓取闭环，改动更小且验证信号更直接。
- 2026-04-02：先新增 `metrics_prometheus_endpoint_exposes_standard_text_format` 集成测试并确认其因 `Content-Type` 不符合 `text/plain; version=0.0.4; charset=utf-8` 而失败，再将 `/metrics` 响应头修正为 Prometheus 标准文本格式。
- 2026-04-02：Phase B 第三个最小子项选择“request id / correlation id 请求标识接线”而不是 tracing_subscriber 初始化，因为它能独立形成请求链路可追踪闭环，且可以用真实 HTTP 集成测试直接验证响应头行为。
- 2026-04-02：先新增 `request_identity.rs` 集成测试并确认当前服务既不会生成 `x-request-id`，也不会透传客户端提供的 `x-request-id / x-correlation-id`，再补充 RequestIdentityMiddleware、全路由接线以及 CORS 允许/暴露头。
- 2026-04-02：Phase B 第四个最小子项选择“关键路由 success/error/latency 指标”而不是 tracing_subscriber 初始化，因为当前 `/metrics` 只能看请求量级，仍缺少问题定位最需要的成功率、错误率和时延信号；该缺口可以用真实 HTTP 抓取直接验证。
- 2026-04-02：先新增 `metrics_prometheus_endpoint_exposes_success_error_and_latency_by_operation` 集成测试并确认 `/metrics` 尚未导出按 operation 维度的成功数、错误数和时延指标，再将这些指标真实接到 TrafficMetricsMiddleware 和 Prometheus 文本导出，同时修正 `/api/v1/metrics/operations` 的错误计数口径。
- 2026-04-02：Phase B 最后一个最小子项选择 `tracing_subscriber` 启动初始化，并以真实二进制启动日志作为完成信号；因为 `evif-rest` 已有 `info!` 启动日志但入口未初始化 subscriber，改动面最小且可直接验证。
- 2026-04-02：先新增 `tracing_init.rs` 集成测试并确认 `evif-rest` 二进制启动时没有任何日志输出；补上 `main.rs` 中基于 `RUST_LOG` 的 `tracing_subscriber::fmt()` 初始化后，进一步分析发现日志实际输出到 `stdout` 而非测试最初假设的 `stderr`，因此同步修正验证口径并确认启动日志真实出现。
- 2026-04-02：进入 Phase A 后，先用 `cargo clippy -p evif-rest --all-targets -- -D warnings` 和 `--no-deps` 复核真实失败面，确认“只修 evif-rest”并不能形成最小闭环，因为路径依赖 `evif-core/evif-auth` 仍会一起挡住门禁。
- 2026-04-02：因此将 Phase A 的最佳最小子项调整为 `evif-auth derivable_impls`，这是 mem15 明确列出的独立任务，改动面最小且验证最直接；真实验证命令为 `cargo clippy -p evif-auth --all-targets -- -D warnings` 与 `cargo test -p evif-auth --all-targets -- --nocapture`。
- 2026-04-03：在完成 `evif-auth` 后，继续用 `cargo clippy -p evif-rest --all-targets --no-deps --message-format short -- -D warnings` 抽取 `evif-rest` 自身失败面，并按“先库代码、再测试代码”两批收口 unused import / dead code / module cleanliness。
- 2026-04-03：`cargo clippy -p evif-rest --all-targets --no-deps -- -D warnings` 仍会打印依赖 crate warning，因此对 `evif-rest` 子项的完成验证改用“过滤后无 `crates/evif-rest` 诊断”加 `cargo test -p evif-rest --lib --tests --quiet` 全绿的组合口径；这代表 `evif-rest` 自身门禁已清理完毕，但全 workspace Phase A 仍未完成。
- 2026-04-03：Phase A 的第三个最小子项选择 `evif-core` clippy 清理；真实失败面主要由 imports、变量、derive/default、checked_div、测试断言、module_inception 与 test module 布局构成，适合按“先机械项、再测试/布局项”两批收口。
- 2026-04-03：在 `evif-core` 清理过程中，额外发现 `cache` 统计测试对 moka `entry_count()` 的即时行为假设过强，会导致测试失败；因此将断言收回到当前实现稳定承诺的 `hits/misses` 与可观测读取结果，再完成 `cargo clippy -p evif-core --all-targets -- -D warnings` 和 `cargo test -p evif-core --all-targets -- --nocapture` 的真实验证。
- 2026-04-03：全 workspace 严格门禁复跑后，已先清掉 `evif-mcp`、`tests/common`、`api/e2e` 测试辅助以及部分 `evif-cli/fuse` 的机械 clippy；当前剩余失败面已明确收敛到 `evif-mem`、`evif-plugins`、`evif-cli`、`evif-fuse` 四块主阻塞，其中 `evif-mem` 仍是最大头。
- 2026-04-03：为完成 Phase A 最后一格，继续真实收口了 `evif-mem`、`evif-plugins`、`evif-cli`、`evif-fuse`、`evif-client`、`tests/api`、`tests/cli`、`tests/e2e`、`evif-mcp/tests`、`evif-bench`、示例插件等剩余门禁与测试问题；其中关键修复包括 `evif-client` 对齐当前 `evif-rest` 契约，以及三组集成测试统一改为自举本地测试服务。
- 2026-04-03：Phase A 已按 mem15 口径完成 100%。真实最终验收命令为 `cargo clippy --workspace --all-targets -- -D warnings` 与 `cargo test --workspace --all-targets -- --nocapture`，二者均已退出 0。
- 2026-04-03：Phase C 的最佳最小子项选择 `TenantState` 持久化，而不是先做 `SyncState` 或 `EncryptionState`，因为租户数据结构最小、边界最清晰，也最容易做成“重启恢复”的真实闭环。
- 2026-04-03：新增 `TenantState::persistent` 与 `create_routes_with_tenant_state`，并让默认路由支持通过 `EVIF_REST_TENANT_STATE_PATH` 启用租户状态文件持久化；真实验证命令为 `cargo test -p evif-rest --test multi_tenant -- --nocapture` 与 `cargo test -p evif-rest --lib --tests --quiet`。
- 2026-04-03：Phase C 的第二个最小子项选择 `SyncState` 持久化版本存储，沿用 `TenantState` 的 JSON 快照模式，只补 `version / pending_changes / tracked_paths` 的可恢复闭环，不提前扩展到冲突治理或多节点同步。
- 2026-04-03：先在 `incremental_sync.rs` 新增 `sync_persistence_survives_restart` 回归测试，并通过其编译失败确认当前缺少 `SyncState::persistent` 与 `create_routes_with_sync_state`；随后补齐默认路由的 `EVIF_REST_SYNC_STATE_PATH` 接线与显式注入入口。
- 2026-04-03：`SyncState` 持久化的真实验收命令为 `cargo test -p evif-rest --test incremental_sync sync_persistence_survives_restart -- --nocapture`、`cargo test -p evif-rest --test incremental_sync -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings` 与 `cargo test -p evif-rest --lib --tests --quiet`，均已退出 0。
- 2026-04-03：Phase C 的第三个最小子项选择 `EncryptionState` 的可恢复配置持久化，不做完整密钥管理系统，只补“启停状态 + key_source + 可恢复 key_reference”的 JSON 快照闭环。
- 2026-04-03：对加密状态持久化采用保守策略：仅 `env:KEY_NAME` 这类可恢复 key 引用支持跨重启恢复；显式提供的裸 key 不写入状态文件，避免把原始密钥以明文形式落盘。
- 2026-04-03：先在 `encryption_at_rest.rs` 新增 `encryption_persistence_survives_restart_with_env_key` 回归测试，并通过其编译失败确认当前缺少 `EncryptionState::persistent` 与 `create_routes_with_encryption_state`；随后补齐 `EVIF_REST_ENCRYPTION_STATE_PATH` 接线与显式注入入口。
- 2026-04-03：`EncryptionState` 持久化的真实验收命令为 `cargo test -p evif-rest --test encryption_at_rest encryption_persistence_survives_restart_with_env_key -- --nocapture`、`cargo test -p evif-rest --test encryption_at_rest -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings` 与 `cargo test -p evif-rest --lib --tests --quiet`，均已退出 0。
- 2026-04-03：Phase C 的最后一个最小子项选择把“哪些状态允许内存态、哪些在生产模式下必须持久化”落实到启动期校验，而不是新增管理接口；这样可以直接复用现有 `validate_memory_for_production` 的模式，以最小改动把规则变成真实门禁。
- 2026-04-03：生产模式下将 `TenantState / SyncState / EncryptionState` 统一归为“必须持久化”的运行态，要求分别配置 `EVIF_REST_TENANT_STATE_PATH / EVIF_REST_SYNC_STATE_PATH / EVIF_REST_ENCRYPTION_STATE_PATH`；`MemoryState` 继续沿用既有 SQLite 持久化要求。
- 2026-04-03：先在 `server.rs` 新增 `test_validate_runtime_state_for_production_env` 回归测试，并通过其编译失败确认当前缺少运行态状态生产校验；随后补齐 `validate_runtime_state_for_production` 并接入 `EvifServer::run` 启动链路。
- 2026-04-03：运行态状态生产校验的真实验收命令为 `cargo test -p evif-rest test_validate_runtime_state_for_production_env -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings` 与 `cargo test -p evif-rest --lib --tests --quiet`，均已退出 0；至此 Phase C 的 5 个明确子项全部完成。
- 2026-04-03：Phase D 的最佳最小子项选择“先收紧最高风险的安全配置端点”，优先把 `/api/v1/encryption/enable` 与 `/api/v1/encryption/disable` 纳入 `admin` 能力分级，而不是一开始就扩展整套契约映射或全量安全端点矩阵。
- 2026-04-03：先在 `auth_protection.rs` 新增 `test_encryption_enable_requires_admin_scope` 回归测试，并通过其失败确认当前 `write-key` 仍可直接落到加密 handler；随后仅补 `route_requirement` 的最小映射，将加密开关端点提升为 `admin` 路由。
- 2026-04-03：为避免并发测试互相污染，`test_encryption_enable_requires_admin_scope` 改用每次生成唯一环境变量名来驱动 `env:KEY_NAME` 场景，而不是固定复用 `EVIF_ENCRYPTION_KEY`。
- 2026-04-03：本轮 Phase D 子项的真实验收命令为 `cargo test -p evif-rest --test auth_protection test_encryption_enable_requires_admin_scope -- --nocapture`、`cargo test -p evif-rest --test auth_protection -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings` 与 `cargo test -p evif-rest --lib --tests --quiet`，均已退出 0。
- 2026-04-03：Phase D 的第二个最小子项选择“把 env 驱动的 API key 与 audit log 链路做成真实 E2E”，优先验证 `RestAuthState::from_env` 与 `EVIF_REST_AUTH_AUDIT_LOG`，而不是先扩更大的 GraphQL/REST/MCP 契约对齐面。
- 2026-04-03：先在 `auth_protection.rs` 新增 `test_auth_from_env_writes_audit_log_file_for_denied_and_granted_requests` 回归测试，并通过其失败确认当前文件型 audit logger 在嵌套目录场景下不会真实落盘。
- 2026-04-03：最小修复落在 `evif-auth` 的 `FileAuditLogger::write_to_file`，写入前自动创建父目录；这样既修复了 REST 鉴权 E2E，也顺手补齐了 auth 基础设施在生产路径下的真实可用性。
- 2026-04-03：在复验过程中额外暴露并修正了 `tracing_init` 的环境继承脆弱性；测试现在会显式清理会影响启动路径的 env 变量，避免安全类 env 测试与黑盒启动测试互相污染。
- 2026-04-03：本轮 Phase D 子项的真实验收命令为 `cargo test -p evif-rest --test auth_protection test_auth_from_env_writes_audit_log_file_for_denied_and_granted_requests -- --nocapture`、`cargo test -p evif-rest --test auth_protection -- --nocapture`、`cargo test -p evif-rest --test tracing_init -- --nocapture`、`cargo clippy -p evif-auth --all-targets -- -D warnings`、`cargo clippy -p evif-rest --all-targets -- -D warnings` 与 `cargo test -p evif-rest --lib --tests --quiet`，均已退出 0。
- 2026-04-03：Phase D 的第三个最小子项选择先收敛 MCP/REST 健康检查契约，而不是一次性改动 GraphQL/REST/MCP 全部状态面；这是当前最小、最容易被真实验证的跨接入面契约漂移。
- 2026-04-03：先在 `evif-mcp` 中新增 `test_evif_health_calls_rest_v1_health_contract` 回归测试，并通过其失败确认 `evif_health` 仍在访问旧的 `/health` 路径，而非 `mcp_protocol` 中约定的 `/api/v1/health`。
- 2026-04-03：最小修复仅将 `evif_health` 切换到 `/api/v1/health`，使 MCP 健康检查与 REST v1/evif-client 统一为 `status / version / uptime` 契约，不顺手扩展 GraphQL 健康查询模型。
- 2026-04-03：同轮额外清理了 `crates/evif-mcp/tests/mcp_phase15.rs` 中影响 `clippy -D warnings` 的 `Default` 后字段再赋值写法，保证 Phase D 契约修复不会留下门禁债务。
- 2026-04-03：本轮 Phase D 子项的真实验收命令为 `cargo test -p evif-mcp test_evif_health_calls_rest_v1_health_contract -- --nocapture`、`cargo test -p evif-mcp --lib -- --nocapture`、`cargo test -p evif-mcp --test mcp_phase15 -- --nocapture` 与 `cargo clippy -p evif-mcp --all-targets -- -D warnings`，均已退出 0。
- 2026-04-03：Phase D 的第四个最小子项选择“先统一 GraphQL 与 REST 的状态词汇”，优先清理最小但真实的 `status.status` 漂移，而不是一次性扩展 GraphQL 字段模型。
- 2026-04-03：先在 `graphql_api.rs` 新增 `graphql_status_matches_rest_health_contract` 回归测试，并通过其失败确认 GraphQL 仍返回 `running`，而 REST v1 health 已返回 `healthy`。
- 2026-04-03：最小修复仅将 GraphQL `status.status` 对齐为 `healthy`，保持 `version` 已有对齐，不顺手增加 `uptime` 或额外 GraphQL 字段，避免超出“最佳最小方式”。
- 2026-04-03：本轮 Phase D 子项的真实验收命令为 `cargo test -p evif-rest --test graphql_api graphql_status_matches_rest_health_contract -- --nocapture`、`cargo test -p evif-rest --test graphql_api -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings` 与 `cargo test -p evif-rest --lib --tests --quiet`，均已退出 0。
- 2026-04-05：按 mem15 Phase D 的“敏感端点能力分级继续收紧”选择最小闭环：优先收紧 `AuthMiddleware::route_requirement`，将租户管理端点 `/api/v1/tenants`（GET/POST）、`/api/v1/tenants/:id`（DELETE）、`/api/v1/tenants/:id/quota`（PATCH）和 `/api/v1/encryption/rotate` 统一提升为 `admin`，不改 handler 业务逻辑。
- 2026-04-05：先在 `auth_protection.rs` 新增 `test_tenant_management_requires_admin_scope` 并确认 RED（write-key 调用 `POST /api/v1/tenants` 返回 200 而非 403），再做最小路由鉴权映射修复，保证遵循 TDD。
- 2026-04-05：本轮真实验收命令为 `cargo test -p evif-rest --test auth_protection test_tenant_management_requires_admin_scope -- --nocapture`、`cargo test -p evif-rest --test auth_protection -- --nocapture`、`cargo test -p evif-rest test_admin_route_classification -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings`，均已退出 0。
- 2026-04-05：在 Phase D 已达 100% 的前提下继续做最小安全补强时，优先选择“收紧加密元数据只读端点”而不是扩更多新接口；因为 `/api/v1/encryption/status` 与 `/api/v1/encryption/versions` 直接暴露全局安全状态与密钥版本历史，风险高于普通只读端点，且只需补 `route_requirement` 即可闭环。
- 2026-04-05：先在 `auth_protection.rs` 新增 `test_encryption_metadata_requires_admin_scope` 并确认 RED（write-key 调用 `GET /api/v1/encryption/status` 返回 200 而非 403），再将 `GET /api/v1/encryption/status` 与 `GET /api/v1/encryption/versions` 提升为 `admin` 路由，保持 handler 逻辑不变，遵循 TDD 的最小实现。
- 2026-04-05：本轮真实验收命令为 `cargo test -p evif-rest --test auth_protection test_encryption_metadata_requires_admin_scope -- --nocapture`、`cargo test -p evif-rest --test auth_protection -- --nocapture`、`cargo test -p evif-rest test_admin_route_classification -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings`，均已退出 0。
- 2026-04-05：按用户要求，本轮完成口径改为“真实功能验收而非测试通过”；因此在继续收紧 Phase D 敏感端点时，优先选择 `/api/v1/tenants/:id`（GET）这一仍可被 `write-key` 读取的真实缺口，而不是继续新增测试覆盖。
- 2026-04-05：变更前已真实启动 `evif-rest`（`cargo run -p evif-rest`，注入 `EVIF_REST_WRITE_API_KEYS / EVIF_REST_ADMIN_API_KEYS / EVIF_REST_AUTH_AUDIT_LOG`），并通过真实 HTTP 请求确认 RED：先用 `admin-key` 创建租户，再用 `write-key` 访问 `GET /api/v1/tenants/:id`，返回 `200`。
- 2026-04-05：最小修复仅收紧 `AuthMiddleware::route_requirement`，将 `GET /api/v1/tenants/:id` 提升为 `admin` 路由，同时显式保留 `GET /api/v1/tenants/me` 作为当前租户自查询入口，不改 tenant handler 业务逻辑。
- 2026-04-05：本轮真实功能验收（非测试）命令为 `EVIF_REST_WRITE_API_KEYS=write-key EVIF_REST_ADMIN_API_KEYS=admin-key EVIF_REST_AUTH_AUDIT_LOG=.codex-runtime-check/auth.log cargo run -p evif-rest`，随后执行真实 `curl`：`POST /api/v1/tenants`（admin=200）、`GET /api/v1/tenants/:id`（write=403, admin=200）、`GET /api/v1/tenants/me`（write=200）；审计日志文件已真实落盘 denied/granted 记录。另补充 `cargo clippy -p evif-rest --all-targets -- -D warnings` 作为代码门禁验证，已退出 0。
- 2026-04-05：本轮转为“全库真实分析”任务时，优先选择先跑 workspace 级硬证据，再做局部服务验收；因为对 EVIF 的价值判断不能只靠阅读代码，必须同时验证 `cargo clippy --workspace --all-targets -- -D warnings` 与 `cargo test --workspace --all-targets -- --nocapture` 的真实结果。
- 2026-04-05：真实执行结果显示 EVIF 当前主干具备较高工程稳定性：workspace clippy 退出 0；workspace test 退出 0；关键 crate 中 `evif-core`、`evif-plugins`、`evif-rest`、`evif-mcp`、`evif-mem`、`evif-cli`、`evif-bench` 均完成真实测试通过。
- 2026-04-05：真实服务验收采用 `cargo run -p evif-rest` + `curl` + `cargo run -p evif-cli`：确认默认挂载真实包含 `/context`、`/skills`、`/pipes`；`GET /api/v1/files?path=/context/L2/architecture.md` 能读出上下文知识；`GET /api/v1/files?path=/skills/code-review/SKILL.md` 能读出技能定义；创建 `/pipes/analysis-task` 并向 `input` 写入后，`status` 真实变为 `running`；CLI `health` 与 `ls /` 能直接使用运行中的 REST 服务。
- 2026-04-05：真实运行 `cargo run -p evif-mcp -- --help` 时并未打印帮助，而是直接启动 MCP server 并加载 26 个工具；这说明 EVIF 的 MCP 面是“agent-first”的实际入口，同时也暴露了一个开发体验缺口：MCP 二进制缺少标准 `--help` 行为。
- 2026-04-05：制定 `mem16.md` 时采用比 `mem15` 更严格的生产平台口径，不再把“最小闭环完成”直接等同于“整个平台可用于生产”；新的评估维度额外纳入了依赖安全、部署资产自洽性、运行时/文档契约一致性、官方 benchmark 对齐度。
- 2026-04-05：本轮新增真实生产级阻塞证据：`cargo audit` 真实发现 22 个漏洞和 7 个 warning；`cargo run -p evif-rest -- --port 3000` 真实仍监听 `8081`；`docker compose -f docker-compose.yml -f docker-compose.prod.yml config` 真实失败；`cargo test -p evif-bench -- --nocapture` 真实输出 `Throughput: 0.0 req/s` 但测试仍通过。
- 2026-04-05：对 EVIF 自带 benchmark 的判断调整为“内部 smoke/perf 价值高，但对外官方 benchmark 对齐度不足”；原因是 `evif-bench` 中多处断言仅验证“不返回 5xx”或 `rate >= 0.0`，尚未接入 OSWorld / AgentBench / SWE-bench / BrowserGym / WorkArena / GAIA / τ-bench / Terminal-Bench / MCP-bench 等官方任务集与评分器。
- 2026-04-05：`mem16.md` 已完成，当前更严格结论为：功能闭环完成度 100%，工程门禁通过度约 90%，生产平台就绪度约 64%（3.2/5），官方 AI benchmark 对齐度约 42%（2.1/5）；后续改造路线图切换到 Phase G-M，优先收口运行时真相、供应链安全、部署资产、端到端覆盖和 benchmark 生产化。
- 2026-04-08：按用户要求忽略 Docker、聚焦核心功能后，优先选择推进 mem17 的 N4“多实例扩展路径”，因为它是当前唯一仍明显卡在核心生产能力上的 HIGH 项，且 `evif-mem` 已有 `PostgresStorage`，最适合做最小真实闭环。
- 2026-04-08：N4 本轮采用“先接通 PostgreSQL 后端路径、再做真实运行验证”的最小实现，而不是直接承诺 HA 完成；因为现有代码缺的是 `evif-rest` 到 `evif-mem/postgres` 的真实接线与验收，而不是再造新的协调层。
- 2026-04-08：先在 `crates/evif-rest/src/memory_handlers.rs` 新增 PostgreSQL 配置/生产校验/真实 Postgres 单测，并通过编译失败确认当前缺少 `MemoryBackendKind::Postgres`、`MemoryBackendConfig::postgres/postgres_url` 以及异步初始化能力，遵循 TDD RED→GREEN。
- 2026-04-08：在把 `evif-rest` 接到 PostgreSQL 后端的真实验证过程中，首次暴露出 `crates/evif-mem/src/storage/postgres.rs` 的时间列类型缺陷：schema 使用 `TIMESTAMP`，但代码按 `chrono::DateTime<Utc>` 读取；因此同步修正为 `TIMESTAMPTZ`，这是由真实读写而非静态阅读发现的问题。
- 2026-04-08：本轮真实验收命令为 `cargo test -p evif-rest --lib test_postgres_memory_backend_round_trips_real_requests -- --nocapture`、`cargo test -p evif-rest --lib -- --nocapture`、`cargo clippy -p evif-rest -p evif-mem --all-targets -- -D warnings`、`cargo test --workspace --all-targets --quiet`、`cargo clippy --workspace --all-targets -- -D warnings`，以及“本地临时 Postgres + `cargo run -p evif-rest` + 带 `x-api-key: write-key` 的真实 `curl POST/GET /api/v1/memories`”。
- 2026-04-08：对 mem17 的最新客观判断调整为：N4 只能算“部分完成”而不是“已完成”，因为 PostgreSQL 路径已经真实可运行，但 3 实例并发一致性、分布式锁/选主/故障转移都尚未实现；因此综合生产就绪度从 61% 上调到约 64%，而不是直接宣称达到 65%+ 的 HA 水平。
- 2026-04-08：在继续推进 N4 时，优先选择“把 PostgreSQL 连接池参数接入 REST 层，并补 3 节点共享同一 Postgres 的真实一致性验收”，因为这比直接引入分布式锁/选主更小、更可验证，也更符合 mem17 当前阶段的最佳最小实现。
- 2026-04-08：先在 `crates/evif-rest/src/memory_handlers.rs` 新增 `backend_description` 的 RED 测试，并通过其编译失败确认当前 `MemoryState` 尚未暴露 PostgreSQL pool 配置摘要；随后补上 `backend_description`、`postgres_pool_bounds` 存储，以及 `MemoryState::from_config` 对 `PostgresStorage::with_options` 的真实接线，遵循 TDD。
- 2026-04-08：N4 本轮新增运行时配置：`EVIF_REST_MEMORY_POSTGRES_MAX_CONNECTIONS` 与 `EVIF_REST_MEMORY_POSTGRES_MIN_CONNECTIONS`。当前默认值为 `10/0`，且在 env 解析阶段会拒绝 `min > max` 的非法组合。
- 2026-04-08：本轮新增真实验收命令为 `cargo test -p evif-rest --lib test_postgres_memory_backend_description_includes_pool_bounds -- --nocapture`、`cargo test -p evif-rest --test postgres_distributed postgres_memory_backend_supports_three_nodes_with_bounded_pool -- --nocapture`，以及“本地临时 Postgres + 3 个 `cargo run -p evif-rest` 进程 + `EVIF_REST_MEMORY_POSTGRES_MAX_CONNECTIONS=2`/`MIN_CONNECTIONS=1` + 真实 `curl POST/GET /api/v1/memories`”。
- 2026-04-08：对 mem17 的客观判断再次上调但仍保持保守：N4 已从“PostgreSQL 可运行路径”推进到“bounded pool + 3 节点共享 Postgres 的真实一致性验证通过”，综合生产就绪度从约 64% 上调到约 66%；但由于仍缺高压并发、故障注入、分布式协调与故障转移，依然不能把这条能力宣称为 HA 完成。
- 2026-04-08：继续按 mem17 聚焦核心功能时，优先选择 Area 3 的“请求体大小限制”而不是 Docker/部署清理，因为 oversized JSON/body 会直接造成 DoS/内存压力，并且可以用真实 HTTP 413 明确验收。
- 2026-04-08：先新增 `crates/evif-rest/tests/request_body_limit.rs`，设置 `EVIF_REST_MAX_BODY_BYTES=128` 后发送超过限制但低于 axum 默认限制的 JSON，请求 initially 返回 200，确认 RED；随后在 `routes.rs` 全局接入 `DefaultBodyLimit::max(max_body_bytes_from_env())`，默认 1MiB。
- 2026-04-08：Area 3 body limit 的真实验收命令为 `cargo test -p evif-rest --test request_body_limit -- --nocapture`、`cargo test -p evif-rest --lib -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings`，以及 `EVIF_REST_MAX_BODY_BYTES=128 EVIF_REST_WRITE_API_KEYS=write-key cargo run -p evif-rest` 后用 oversized `curl POST /api/v1/memories` 确认真实返回 413。
- 2026-04-08：对 mem17 的客观判断更新为：请求体大小限制已形成真实闭环，DoS 防护分项从“并发限制已完成但 body limit 缺失”推进到“并发限制 + body limit 已完成”；综合生产就绪度从约 66% 上调到约 67%，但安全响应头和 JSON body path traversal 校验仍未完成。
- 2026-04-11：继续按 mem17 聚焦核心功能时，优先选择 Area 2 中剩余的 `per-API-key` 限速和 `x-ratelimit-*` 响应头，而不是继续做更大的鉴权栈改造；因为它是当前 DoS/滥用防护里最小、最容易形成真实 HTTP 闭环的一格。
- 2026-04-11：先在 `crates/evif-rest/src/middleware.rs` 新增两个 RED 测试：一个验证成功响应会返回 `x-ratelimit-limit/remaining`，一个验证同一 API key 在并发上限为 1 时第二个 in-flight 请求返回 `429`；初始编译失败仅因测试缺少 `axum::middleware` 导入，修正后测试开始真正约束功能。
- 2026-04-11：本轮实现采用 `RestAuthState` 内部持有按 principal_id 分桶的 semaphore registry，配置项为 `EVIF_REST_API_KEY_MAX_CONCURRENT_REQUESTS`；默认未配置时不启用，配置后对受保护路由生效，并在成功/拒绝响应中统一写入 `x-ratelimit-limit` 与 `x-ratelimit-remaining`。
- 2026-04-11：Area 2 API key 限速的真实验收命令为 `cargo test -p evif-rest --lib test_api_key_rate_limit_headers_are_present -- --nocapture`、`cargo test -p evif-rest --lib test_api_key_rate_limit_rejects_second_inflight_request -- --nocapture`、`cargo test -p evif-rest --lib -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings`，以及 `EVIF_REST_API_KEY_MAX_CONCURRENT_REQUESTS=1 EVIF_REST_WRITE_API_KEYS=write-key cargo run -p evif-rest` 后，使用一个慢上传 `curl` 占住 permit，再对同一路由发第二个同 key 请求，真实得到 `429` 和 `x-ratelimit-*` 头。
- 2026-04-11：对 mem17 的客观判断再次上调但保持保守：DoS 防护现在已从“全局并发限制 + body limit”推进到“再加 per-API-key 并发限制和响应头”，综合生产就绪度从约 67% 上调到约 68%；但 per-IP 限速、JWT/OAuth、API key 哈希，以及 N4 的高压并发/故障场景一致性仍未完成。
- 2026-04-12：继续按 mem17 收口 Area 2 时，优先选择补 `per-IP` 限速，而不是马上做更重的 JWT/OAuth 改造；因为 DoS 防护仍缺一个最小但重要的维度，而且可以与现有 `429 + x-ratelimit-*` 机制复用形成真实闭环。
- 2026-04-12：先在 `crates/evif-rest/src/middleware.rs` 新增 `test_ip_rate_limit_isolated_per_client_ip`，用 `x-real-ip` 模拟两个来源；首次 RED 通过编译失败确认当前缺少 `IpRateLimitState` 和 `IpRateLimitMiddleware`，随后补上按 IP 分桶的 semaphore registry，并在 `routes.rs` 的 `attach_request_identity` 中全局接入。
- 2026-04-12：在真实服务验证中额外暴露一个细节 bug：IP 限流命中时虽然真实返回 `429`，但错误消息误写成了 `API key concurrency limit exceeded`；因此先补 RED 断言消息文本，再把限流响应函数改成可区分 API key / IP 两种文案，避免把错误原因报错给用户。
- 2026-04-12：Area 2 IP 限速的真实验收命令为 `cargo test -p evif-rest --lib test_ip_rate_limit_isolated_per_client_ip -- --nocapture`、`cargo test -p evif-rest --lib -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings`，以及 `EVIF_REST_IP_MAX_CONCURRENT_REQUESTS=1 EVIF_REST_WRITE_API_KEYS=write-key cargo run -p evif-rest` 后，使用 `x-real-ip: 203.0.113.10` 的慢上传 `curl` 占住 permit，再发第二个同 IP 请求和一个不同 IP 请求，真实得到 `429/200` 与 `x-ratelimit-*` 头。
- 2026-04-12：对 mem17 的客观判断再次上调但保持保守：DoS 防护现在已从“全局并发限制 + body limit + per-key 限速”推进到“再加 per-IP 限速”，综合生产就绪度从约 68% 上调到约 69%；但 trusted proxy 校验、`Retry-After`/reset 语义、JWT/OAuth、API key 哈希，以及 N4 的高压并发/故障场景一致性仍未完成。
- 2026-04-13：继续按 mem17 收口 Area 10 时，优先选择“支持哈希 API key”而不是直接切到 JWT/OAuth；因为这是当前最小、最能直接提升静态 key 安全性的能力，而且可以在不破坏现有明文 env 兼容性的前提下形成真实闭环。
- 2026-04-13：先在 `crates/evif-rest/tests/auth_protection.rs` 新增 `test_auth_from_env_accepts_sha256_hashed_api_keys`；初始 RED 真实失败为“只配置 `*_API_KEYS_SHA256` 时原始 `x-api-key` 仍拿不到 200”，确认当前 `RestAuthState::from_env` 只支持明文 key。
- 2026-04-13：本轮实现采用“双索引”方式：`RestAuthInner` 同时维护明文 key map 与 SHA-256 hash map；配置项为 `EVIF_REST_WRITE_API_KEYS_SHA256` 和 `EVIF_REST_ADMIN_API_KEYS_SHA256`，请求到来时若明文 key 未命中，则对传入 key 做 SHA-256 后再查哈希表，从而保持对现有明文 env 的兼容。
- 2026-04-13：Area 10 API key 哈希的真实验收命令为 `cargo test -p evif-rest --test auth_protection test_auth_from_env_accepts_sha256_hashed_api_keys -- --nocapture`、`cargo test -p evif-rest --test auth_protection -- --nocapture`，以及 `EVIF_REST_WRITE_API_KEYS_SHA256=<sha256(write)> EVIF_REST_ADMIN_API_KEYS_SHA256=<sha256(admin)> cargo run -p evif-rest` 后，使用无效 key 和原始 write/admin key 分别请求，真实得到 `401/200/200`。
- 2026-04-13：对 mem17 的客观判断再次上调但保持保守：鉴权增强已从“仅明文静态 key”推进到“支持哈希静态 key + per-key/per-IP 并发限制”，综合生产就绪度从约 69% 上调到约 70%；但 JWT/OAuth、token 生命周期治理、trusted proxy 校验，以及 N4 的高压并发/故障场景一致性仍未完成。
- 2026-04-13：在做哈希 API key 完整复验时，真实 `cargo test --workspace --all-targets --quiet` 再次暴露 `tests/e2e/tests/e2e_rest_api.rs` 的稳定性问题：默认整套执行会出现 5 个失败，但多个失败用例（如 `e2e_04_mount_plugin`、`e2e_10_create_file`）单独运行时又能通过，说明当前更像 E2E harness/共享状态问题，而不是本轮功能直接退化。
- 2026-04-13：因此对 mem17 的最新客观判断改为更保守：哈希 API key 功能本身已完成并通过真实 HTTP 验证，但默认 workspace E2E harness 仍不稳定；综合生产就绪度不再继续上调，维持在约 69%，直到 E2E harness 可靠性和 N4 的高压并发/故障场景验证一起补齐。
- 2026-04-13：继续追查 workspace 验证不稳定时，先确认 `tests/e2e/tests/e2e_rest_api.rs` 在 crate 级执行和单用例执行中的行为；最终定位到根因不是最近功能改动，而是这组 E2E 依赖固定端口 `127.0.0.1:8081`，并在测试进程内共享同一内嵌服务状态，导致默认并行门禁下会出现端口冲突和状态串扰。
- 2026-04-13：修复策略采用最小但结构性正确的方式：`tests/e2e/tests/e2e_rest_api.rs` 改为自举到随机端口，并把 base URL 存进 `OnceLock<String>`；同时用 `tokio::Mutex` 让 `TestContext::new().await` 持有串行 guard，避免该测试二进制内部的共享状态互相踩。
- 2026-04-13：在继续复验中又暴露了 `crates/evif-rest/tests/tracing_init.rs` 的脆弱性：它只睡 800ms 然后赌 stdout/stderr 有日志。当前实现已改为设置临时 `EVIF_LOG_DIR` 和 `EVIF_REST_PORT=0`，轮询日志文件内容直到出现 `Loading plugins`，从而消除并行环境下的时序抖动。
- 2026-04-13：本轮全量真实验收结果更新为：`cargo test -p e2e-tests --test e2e_rest_api -- --nocapture` 通过（27 passed, 4 ignored）、`cargo test -p evif-rest --test tracing_init rest_binary_emits_startup_logs_when_started -- --nocapture` 通过、`cargo test --workspace --all-targets --quiet` 退出 0、`cargo clippy --workspace --all-targets -- -D warnings` 退出 0。
- 2026-04-13：基于上述修复，对 mem17 的客观判断恢复上调：哈希 API key 功能本身已完成，workspace 默认 E2E harness 稳定性也已修复，因此综合生产就绪度从保守的约 69% 调回约 70%；当前最主要的剩余核心缺口重新收敛到 N4 的高压并发/节点故障场景，以及 JWT/OAuth、token 生命周期治理、trusted proxy 校验与供应链安全。
- 2026-04-14：继续按 mem17 收口 Area 2 时，优先选择补 `Retry-After` 而不是继续扩更多限流维度；因为当前已经能稳定返回 `429` 和 `x-ratelimit-*`，最小的下一个生产化提升就是给客户端明确的退避语义。
- 2026-04-14：先在 `crates/evif-rest/src/middleware.rs` 的 API key / IP 限流测试里新增对 `retry-after: 1` 的断言，确认 RED；随后在 `rate_limit_response` 中统一写入 `Retry-After: 1`，不额外扩展 reset 时间计算，保持最小实现。
- 2026-04-14：Area 2 Retry-After 的真实验收命令为 `cargo test -p evif-rest --lib test_api_key_rate_limit_rejects_second_inflight_request -- --nocapture`、`cargo test -p evif-rest --lib test_ip_rate_limit_isolated_per_client_ip -- --nocapture`、`cargo test -p evif-rest --lib -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings`，以及 `EVIF_REST_API_KEY_MAX_CONCURRENT_REQUESTS=1 EVIF_REST_WRITE_API_KEYS=write-key cargo run -p evif-rest` 后用慢上传 `curl` 触发 `429`，真实观测到 `retry-after: 1` 和 `x-ratelimit-*` 头。
- 2026-04-14：对 mem17 的客观判断小幅上调：DoS 防护现在已从“能限流”推进到“能限流且能给客户端明确退避语义”，综合生产就绪度从约 70% 上调到约 71%；剩余核心缺口仍是 N4 的高压并发/节点故障场景、JWT/OAuth、token 生命周期治理、trusted proxy 校验和供应链漏洞清理。
- 2026-04-14：继续按 mem17 推进 N4 时，优先选择“并发写入无丢数”的最小高压场景，而不是直接上故障注入；因为这是当前共享 Postgres 路径最容易形成真实闭环的一步，也能比“3 节点各写 1 次”更接近生产负载。
- 2026-04-14：先在 `crates/evif-rest/tests/postgres_distributed.rs` 新增 `postgres_memory_backend_preserves_writes_under_concurrent_three_node_load`，让 3 个节点各发 10 次并发写入；该测试一次就通过，说明当前这条路径至少在 30 次并发写入量级上没有出现丢数。
- 2026-04-14：随后补做真实服务验收：启动 3 个 `cargo run -p evif-rest` 进程，共享同一 Postgres，使用 30 次并发 HTTP 写入后从其中一个节点读取，真实得到 `count=30`，与写入数一致。
- 2026-04-14：基于上述真实证据，对 mem17 的客观判断再次小幅上调：N4 已从“共享 Postgres + 3 节点验证”推进到“共享 Postgres + 3 节点并发写入无丢数”，综合生产就绪度从约 71% 上调到约 72%；但节点故障/重连抖动、选主/协调能力、JWT/OAuth、token 生命周期治理、trusted proxy 校验和供应链漏洞清理仍未完成。
- 2026-04-17：继续按 mem17 推进 N4 时，优先选择“Postgres 短暂不可用/恢复后同一 REST 节点可继续工作”而不是直接引入协调组件；因为这是在现有共享 Postgres 路径上最小、最真实的故障恢复验证。
- 2026-04-17：先在 `crates/evif-rest/tests/postgres_distributed.rs` 中补充 `TestPostgresInstance::stop/restart`，再新增 `postgres_memory_backend_recovers_after_database_restart`：先写入 `before-db-restart`，停止并重启 Postgres，然后用同一个 REST 节点写入 `after-db-restart` 并读取两条数据。
- 2026-04-17：N4 数据库重启恢复的真实验收命令为 `cargo test -p evif-rest --test postgres_distributed postgres_memory_backend_recovers_after_database_restart -- --nocapture`、`cargo test -p evif-rest --test postgres_distributed -- --nocapture`、`cargo clippy -p evif-rest --all-targets -- -D warnings`，以及“`cargo run -p evif-rest` + 临时 Postgres stop/start + `curl POST/GET /api/v1/memories`”，真实读取到 `before-db-restart-real` 与 `after-db-restart-real`。
- 2026-04-17：基于上述真实证据，对 mem17 的客观判断再次小幅上调：N4 已覆盖共享 Postgres、bounded pool、3 节点一致性、并发写入无丢数、REST 节点重启恢复和 Postgres stop/start 恢复；综合生产就绪度从约 72% 上调到约 73%。仍未完成的是选主/协调、完整故障转移策略、JWT/OAuth、token 生命周期治理、trusted proxy 校验和供应链漏洞清理。
- 2026-04-25：按用户要求对 `/Users/louloulin/Documents/linchong/claw/agfs` 做了真实代码级对标，不再沿用历史“EVIF 仍缺大量核心 FS 原语”的旧判断；本轮结论写入 `mem18.md`：EVIF 在核心插件面与接入面已基本追平甚至局部超过 AGFS，最真实的剩余差距已转为专用 shell/脚本运行时、多语言 SDK、agent workflow demos、安装分发与插件产品化可信度。
- 2026-04-25：后续路线需要分开处理，避免混淆“对标 AGFS 的开发者产品化差距”和“对标生产平台的可靠性差距”；若继续对标 AGFS，优先级应改为 Python SDK、agent task loop demos、shell/script 体验、quick start/installer 与插件兼容矩阵，而不是继续优先补新的 FS 名称。
